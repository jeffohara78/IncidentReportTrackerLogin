# IncidentReportTrackerLogin
A C# console app that has been upgraded by adding a user authentication system with login functionality, secure password hashing, password salts, and persistent user storage using a separate users.json file. The application now requires users to authenticate before accessing the incident management features.
